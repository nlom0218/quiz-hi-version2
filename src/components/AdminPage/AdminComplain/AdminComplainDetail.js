import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  grid-column: 1 / -1;
`

const AdminComplainDetail = () => {
  return (<Container>AdminComplainDetail</Container>);
}

export default AdminComplainDetail;